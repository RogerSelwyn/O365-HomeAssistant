name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Black Check
      uses: jpetrucciani/black-check@19.10b0
      with:
        path: ./custom_components/office365calendar/sensor.py
